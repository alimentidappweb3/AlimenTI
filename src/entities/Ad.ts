export interface Ad {
    adId?: number,
    tittle: string,
    description: string,
    quantity: number,
    limQuantity: number,
    date: string,
    isOpen: boolean,
    localization: string,
    marketAddress: string,
}